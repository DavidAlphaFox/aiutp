{erl_opts, [debug_info]}.
{pre_hooks,
  [{ compile, "cmake ."},
   { compile, "cmake --build ."}]}.
{post_hooks,
  [{clean, "rm -rf CMakeFiles"},
   {clean, "rm -rf CMakeCache.txt"},
   {clean, "rm -rf Makefile"}]}.
{deps, []}.
